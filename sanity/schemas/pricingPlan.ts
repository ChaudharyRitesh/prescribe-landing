/**
 * Retired Sanity pricingPlan schema.
 * 
 * We have migrated the SaaS pricing catalog from static CMS schemas to 
 * a dynamic and active MongoDB + Express backend API (`GET /api/catalog`), 
 * allowing seamless pricing adjustments and automated super-admin catalog management.
 */
export default {};
