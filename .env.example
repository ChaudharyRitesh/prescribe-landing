# SMTP settings (required for /api/newsletter and /api/enterprise-inquiry)
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=465
SMTP_USER=your-smtp-username-or-email
SMTP_PASS=your-smtp-password

# Optional routing (recommended)
# Where enterprise inquiries should go:
ENTERPRISE_INQUIRY_TO=support@kaerpgroup.com
# Where newsletter signups should go:
NEWSLETTER_TO=support@kaerpgroup.com

# Optional fallbacks (used if specific *_TO vars aren't set)
SUPPORT_EMAIL=support@kaerpgroup.com
CONTACT_SALES_TO=support@kaerpgroup.com

# Optional debugging (prints SMTP diagnostics in server logs)
DEBUG_EMAIL=false
